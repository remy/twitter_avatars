# Download twitter avatars

## Install

    npm install -g get-twitter-avatars

## Usage

    get-twitter-avatars [somefile]

The `somefile` is a comma separated list of twitter handles, i.e.

    rem,leftlogic,forbind,js_bin

The command will create a directory called `avatars` and dowload the large avatars for each user there.